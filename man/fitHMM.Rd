% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitHMM.R
\name{fitHMM}
\alias{fitHMM}
\title{Fit Hidden Markov Model for copy number calling}
\usage{
fitHMM(
  copy,
  selftransitionprob = 0.99,
  maxCN = 15,
  sd_value = 0.2,
  usecpp = TRUE
)
}
\arguments{
\item{copy}{Numeric vector of copy number values (e.g., GC-corrected read counts)}

\item{selftransitionprob}{Probability of staying in the same copy number state (default: 0.99)}

\item{maxCN}{Maximum copy number state to consider (default: 15)}

\item{sd_value}{Standard deviation for the normal emission model (default: 0.2)}

\item{usecpp}{Logical, whether to use C++ implementation of Viterbi algorithm (default: TRUE)}
}
\value{
Integer vector of inferred copy number states (0-based indexing)
}
\description{
Fits a Hidden Markov Model to copy number data using the Viterbi algorithm
to find the most likely sequence of copy number states.
}
\details{
The function models copy number states as hidden states in an HMM where:
\itemize{
\item States represent integer copy numbers from 0 to maxCN
\item Emission probabilities follow a normal distribution centered on the true copy number
\item Transition probabilities favor staying in the same state (selftransitionprob)
\item The Viterbi algorithm finds the most likely state sequence
}
}
\examples{
# Generate some example copy number data
copy_data <- c(rep(2, 50), rep(4, 30), rep(2, 20))
copy_data <- copy_data + rnorm(length(copy_data), 0, 0.2)

# Fit HMM
states <- fitHMM(copy_data, selftransitionprob = 0.99, maxCN = 6)

}
